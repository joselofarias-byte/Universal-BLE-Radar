import 'package:flutter_test/flutter_test.dart';
import 'package:universal_ble_radar/main.dart';

void main() {
  test('SignalRadarApp can be constructed', () {
    expect(const SignalRadarApp(), isA<SignalRadarApp>());
  });
}
